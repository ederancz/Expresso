# Milestones 1–2 (Allen ABC Atlas notebooks). Requires Python 3.11 or 3.12 (see environment.yml).
# Install: conda env create -f environment.yml  OR  pip install -r requirements.txt
git+https://github.com/alleninstitute/abc_atlas_access.git
anndata>=0.10
pandas>=2.0
numpy>=1.26,<2
scipy>=1.12
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0
pyarrow>=14.0
tqdm
